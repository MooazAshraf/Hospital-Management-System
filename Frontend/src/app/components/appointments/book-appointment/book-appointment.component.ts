import { Component } from '@angular/core';

// TODO(Person 5): build the real booking form (department, doctor, date,
// time, reason) with FormGroup + validators, POST to /api/appointments,
// then route to the payment step.
@Component({
  selector: 'app-book-appointment',
  standalone: true,
  templateUrl: './book-appointment.component.html'
})
export class BookAppointmentComponent {}
